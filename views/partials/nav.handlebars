<header>
    <nav class="navbar navbar-default standard-headbar">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">LearnR</a>
            </div>
            <div>
                <div class="navbar-collapse">
                    <div class="row">
                        <div class="col-sm-offset-3 col-sm-4">
                            <!-- update the placeholder depending on whether the user is logged in,
								 as only logged in users can run a full search -->
                            {{#if loggedIn}}
                            <input id="search-text" type="text" class="form-control" placeholder="Search..." maxlength="20"> {{else}}
                            <input id="search-text" type="text" class="form-control" placeholder="Search Classes..." maxlength="20"> {{/if}}
                            <div id="search-dropdown">
                                <!-- to be filled in through javascript with the search suggestions -->
                            </div>
                        </div>


                        {{#if loggedIn}}
                        <div>
                            <ul class="nav navbar-nav navbar-right">
                                <li><a href="/messaging">Messaging</a></li>
                                <li><a href="/createcourse">Create a Course</a></li>
                                <li>
                                    <a class="nopadding" href="/user/profile"><img src="/img/profile.png" alt="Profile" height="40" width="40" class="profile_icon"></a>
                                </li>
                                <a id="logout-anchor" class="standard-clear-button" href="/">Logout</a>
                            </ul>
                        </div>

                        {{else}}
                        <div class="navbar-collapse">
                            <ul class="nav navbar-nav navbar-right">
                                <li><a id="userlogin_btn" href="#">Login</a></li>
                                <li>
                                    <button type="button" id="signup_btn" class="standard-blue-button">Sign Up</button>
                                </li>
                            </ul>
                        </div>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </nav>
</header>